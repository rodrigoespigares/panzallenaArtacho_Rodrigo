<form method="post" action="{{ path('app_producto_delete', {'codProd': producto.codProd}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.codProd) }}">
    <button class="btn">Delete</button>
</form>
