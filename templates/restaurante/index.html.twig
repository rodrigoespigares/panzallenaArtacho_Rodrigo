{% extends 'base.html.twig' %}

{% block title %}Restaurante{% endblock %}

{% block body %}
    <h1>Restaurante</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Cod_res</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Contraseña</th>
                <th>Verificado</th>
                <th>Opciones</th>
            </tr>
        </thead>
        <tbody>
        {% for restaurante in restaurantes %}
            <tr>
                <td>{{ restaurante.codRes }}</td>
                <td>{{ restaurante.email }}</td>
                <td>{{ restaurante.roles ? restaurante.roles|json_encode : '' }}</td>
                <td>{{ restaurante.password }}</td>
                <td>{{ restaurante.isVerified ? 'Si' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_restaurante_show', {'cod_res': restaurante.cod_res}) }}">show</a>
                    <a href="{{ path('app_restaurante_edit', {'cod_res': restaurante.cod_res}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">No hay resultados</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_restaurante_new') }}">Crear uno nuevo</a>
{% endblock %}
